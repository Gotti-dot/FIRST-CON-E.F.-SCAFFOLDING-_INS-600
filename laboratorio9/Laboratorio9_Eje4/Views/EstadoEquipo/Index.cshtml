@model IEnumerable<Laboratorio9_Eje4.Models.EstadoEquipo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DesEst)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DesEst)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodEst }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodEst }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodEst })
        </td>
    </tr>
}

</table>
